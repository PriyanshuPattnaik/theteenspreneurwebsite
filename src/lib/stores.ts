// Store management for TheTeenspreneur
import { writable } from 'svelte/store';

// Example stores - will be expanded later
export const isMenuOpen = writable(false);
export const currentPage = writable('home');